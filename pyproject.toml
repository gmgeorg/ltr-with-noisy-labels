[project]
name = "ltr-with-noisy-labels"
version = "0.1.1"
description = "Learning to rank (LTR) with noisy labels"
readme = "README.md"
requires-python = ">=3.12,!=3.14.1"
dependencies = [
    "choix>=0.4.1",
    "ipython>=9.8.0",
    "networkx>=3.6.1",
    "seaborn>=0.13.2",
    "pandas (>=2.0.0,<3.0.0)",
]

[tool.poetry.group.dev.dependencies]
pytest = "^9.0.2"

